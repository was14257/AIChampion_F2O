# RETFound 파이프라인 의존성
# GPU(CUDA) 환경 기준. torch 는 본인 CUDA 버전에 맞는 빌드를 설치하세요.
#   예) CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
torchvision>=0.16
timm>=0.9.12
numpy>=1.24
pillow>=10.0
scipy>=1.10
pandas>=2.0
openpyxl>=3.1      # xlsx 읽기
scikit-learn>=1.3  # r2_score, PLS/PCA 등
matplotlib>=3.7
albumentations>=1.4
tqdm>=4.65          # extract.py 진행바
# 선택(볼륨 포맷에 따라, gamma_oct_tier1/extract.py):
#   nibabel>=5.0    # .nii(.gz) 볼륨을 쓸 때
